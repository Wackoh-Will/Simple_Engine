package Powers;

import main.GamePanel;
import entity.Player;

import java.awt.*;

public class Health extends Pickup {
  /*
    private int x,y;
    private int width = 32;
    private int height = 32;

    private Rectangle hitbox;

    public MedPack(int x, int y){
        this.x = x;
        this.y = y;
    }
*/
    @Override
    public void pickedup(GamePanel gp, Player p) {
        /*
        if(this.hitbox.intersects(gp.player.getHitbox())) {
            p.health += 50;
            System.out.println("You gained 50 Health!");

        }
        STILL being worked on
         */
    }
}
